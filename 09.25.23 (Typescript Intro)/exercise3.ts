// Write a TypeScript program that calculates and displays the area of a rectangle. 
// Use variables length and width to store the dimensions of the rectangle.

const len: number = 25; 
const width: number = 5;  

const area: number = len * width;

console.log(`${area}`);
