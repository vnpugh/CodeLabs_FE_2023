// Write a typeScript function called multiply that takes two numbers 
// as arguments and returns their product. 

function multiply(x: number, y: number): number {
    return x * y;
  }
  
  const product: number = multiply(18, 3); 
  console.log(product); 